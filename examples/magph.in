&control
  calculation = 'hybrid',
  prefix = 'sample',
  outdir = './slw-tmp',
  verbosity = 'normal'
/

&parallel
  execution = 'auto',
  ! Set the MPI rank count in the launcher; keep rank-local pools disabled.
  workers_per_rank = 1,
  threads_per_worker = 1
/

&magph
  j_tensor_h5 = '${savedir}/sample.j_tensor.h5',
  dj_tensor_h5 = '${savedir}/sample.dj_tensor.h5',
  phonon_cache = '${savedir}/phonon_cache.npz',
  out_dir = '${savedir}',
  output = '${savedir}/magph.npz',
  s = 1.0,
  spin_direction = 0.0, 0.0, 1.0,
  kmesh = 4, 4, 4,
  qpath = 'G 0 0 0 X 0.5 0 0',
  path_points = 101,
  show = .false.
/
