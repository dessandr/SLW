# Archived SOC fitting code

This directory preserves abandoned onsite, offsite, and nonlocal fitting
experiments for reference. The active exchange pipeline does not import these
modules. Public SOC input uses `SOC (atomic)` with Wannier90 projection
manifolds and fixed Wannier90/TB2J conventions.
